/* === Global Layout === */
body, .z-window {
    font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
    background-color: #f5f6fa;
    color: #2c3e50;
}

/* === Sidebar === */
.sidebar {
    background: #2c3e50;
    color: #ecf0f1;
    padding: 20px;
    height: 100%;
}
.sidebar a {
    display: block;
    color: #ecf0f1;
    text-decoration: none;
    margin: 10px 0;
    font-weight: 500;
}
.sidebar a:hover {
    background: #34495e;
    padding: 8px;
    border-radius: 6px;
}

/* === Card / Groupbox === */
.card {
    background: #ffffff;
    border-radius: 10px;
    box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    padding: 15px;
    margin-bottom: 20px;
}

/* === Form === */
.form-label {
    font-weight: bold;
    color: #34495e;
    margin-right: 10px;
}
.z-textbox, .z-combobox {
    border: 1px solid #dcdde1;
    border-radius: 6px;
    padding: 6px;
}
.z-textbox:focus, .z-combobox:focus {
    border-color: #3498db;
    box-shadow: 0 0 4px rgba(52,152,219,0.5);
}

/* === Buttons === */
.btn {
    border: none;
    border-radius: 6px;
    padding: 8px 18px;
    font-weight: bold;
    cursor: pointer;
}
.btn-save {
    background: #2ecc71;
    color: white;
}
.btn-save:hover {
    background: #27ae60;
}
.btn-cancel {
    background: #e74c3c;
    color: white;
}
.btn-cancel:hover {
    background: #c0392b;
}
.btn-edit {
    background: #f39c12;
    color: white;
}
.btn-edit:hover {
    background: #d68910;
}
.btn-info {
    background: #3498db;
    color: white;
}
.btn-info:hover {
    background: #2980b9;
}

/* === Table / Listbox === */
.z-listbox, .z-grid {
    border-radius: 8px;
    overflow: hidden;
}
.z-listheader {
    background: #3498db;
    color: white;
    font-weight: bold;
}
.z-listitem:hover {
    background: #f0f3f4;
}
